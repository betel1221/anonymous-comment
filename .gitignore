# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies and build outputs
node_modules/           # Ignored for the root (your frontend's node_modules)
server/node_modules/    # Ignored for the backend's node_modules
dist/
dist-ssr/

# Environment variables (IMPORTANT!)
.env                    # Generic .env file at root
.env.local              # Local environment overrides
.env.*.local            # Other local environment overrides (e.g., .env.development.local)
server/.env             # Specific to your backend's .env file

# Editor directories and files
.vscode/
!.vscode/extensions.json # Keep extensions.json to share recommended extensions
.idea/                   # For IntelliJ/WebStorm
.DS_Store                # macOS specific
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?